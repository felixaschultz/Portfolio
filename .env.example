# Public site URL for SEO (canonical, Open Graph, sitemap)
SITE_URL=https://www.felix-schultz.dk

# Used by the portfolio site (loaders)
SANITY_PROJECT_ID=your_project_id
SANITY_DATASET=production
SANITY_API_VERSION=2024-05-16
# Read token for the site (optional). For studio uploads, use an Editor token:
SANITY_API_TOKEN=

# Exposed to Sanity Studio in the browser (use Editor permissions for uploads)
# SANITY_STUDIO_API_TOKEN=sk...

# Optional: override studio-only vars (defaults mirror SANITY_* above)
# SANITY_STUDIO_PROJECT_ID=your_project_id
# SANITY_STUDIO_DATASET=production
